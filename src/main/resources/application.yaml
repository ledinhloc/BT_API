server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/uni_docs_test"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL:abc}
    password: ${PASSWORD:12}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true